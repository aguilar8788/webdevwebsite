import axios from 'axios'

export function deleteObject(data) {
    return function(dispatch, getState) {
       console.log("data", data)
        // return axios.post("http://localhost:8080/files", formData.data)
    }
}
